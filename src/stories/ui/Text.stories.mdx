import {ArgsTable, Canvas, Meta, Story, Props} from '@storybook/addon-docs';
import {Text} from '@/ui/styled/Texts';
import {TypoProps} from '@/ui/styled/types/types';
import colors from '@/ui/colors';

<!-- 컴포넌트 정의   type: {required: true} -->

<Meta
  title="UI/Text"
  argTypes={{
    children: {
      table: {disable: true},
      defaultValue: 'Hello World!'
    },
    $color: {
      control: {type: 'color'},
      defaultValue: colors.gray07,
      description: '폰트 컬러',
      table: {
        type: {summary: 'string'},
        defaultValue: {summary: colors.gray07}
      }
    },
    $type: {table: {disable: true}},
    bold: {
      control: {type: 'boolean'},
      defaultValue: false,
      description: '볼드 여부 설정',
      table: {
        type: {summary: 'boolean'},
        defaultValue: {summary: false}
      }
    }
  }}
  component={Text}
/>

export const ComponentTemplate = args => {
  return <Text {...args} />;
};

# Text

p 태그를 쉽게 사용하기 위한 컴포넌트
스타일 정의한 타입값에 따라 폰트속성이 달라진다.

<Canvas>
  <Story name="Default">{ComponentTemplate.bind({})}</Story>
</Canvas>

## Props

<ArgsTable story="Default" />

## Text Types

지원되는 타입 및 폰트 크기(왼쪽부터 h1 ~ h6)

<Canvas>
  <Story
    name="Text Types"
    parameters={{
      controls: {disable: true},
      options: {showPanel: false}
    }}
  >
    <Text $type="h1">Hello!</Text>
    <Text $type="h2">Hello!</Text>
    <Text $type="h3">Hello!</Text>
    <Text $type="h4">Hello!</Text>
    <Text $type="h5">Hello!</Text>
    <Text $type="h6">Hello!</Text>
  </Story>
</Canvas>
