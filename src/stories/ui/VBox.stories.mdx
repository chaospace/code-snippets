import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';
import {VBox} from '@/ui/styled/Box';
import {Text} from '@/ui/styled/Texts';
import {Space} from '@/ui/space';

<!-- 컴포넌트 정의   type: {required: true} -->

<Meta
  title="Layout/VBox"
  argTypes={{
    children: {
      table: {disable: true},
      defaultValue: (
        <>
          <Text $type="h1">VBox Story Example</Text>
          <Text $type="h2">VBox는 요소가 세로로 정렬 됩니다.</Text>
          <Text $type="h3">gap속성 만큼 요소별 공간이 설정됩니다</Text>
          <Text $type="h4">alignItems은 아이템 정렬</Text>
          <Text $type="h5">alignContent은 flex line 정렬</Text>
          <img src="https://www.google.com/images/branding/googlelogo/2x/googlelogo_color_272x92dp.png" />
        </>
      )
    },
    flexWrap: {
      options: ['wrap', 'nowrap'],
      control: {type: 'select'},
      defaultValue: 'nowrap',
      description: '자식요소의 줄바꿈 표현 여부 설정',
      table: {
        type: {
          summary: 'string'
        },
        defaultValue: {summary: 'nowrap'}
      }
    },
    alignItems: {
      options: ['center', 'flex-end', 'flex-start'],
      control: {type: 'select'},
      defaultValue: 'flex-start',
      description: 'flex line을 기준으로 아이템을 정렬',
      table: {
        type: {
          summary: 'string'
        },
        defaultValue: {summary: 'flex-start'}
      }
    },
    alignContents: {
      type: {name: 'string'},
      options: [
        'flex-start',
        'center',
        'flex-end',
        'space-between',
        'space-evenly',
        'space-around'
      ],
      control: {type: 'select'},
      defaultValue: 'flex-start',
      description: 'flex line을 정렬 *flex-wrap설정 필수',
      table: {
        type: {
          summary: 'string'
        },
        defaultValue: {summary: 'flex-start'}
      }
    },
    bgColor: {
      type: {name: 'string'},
      control: 'color',
      description: '배경컬러',
      table: {
        type: {
          summary: 'string'
        },
        defaultValue: {summary: '#fff'}
      }
    },
    gap: {
      type: {name: 'number'},
      control: 'number',
      defaultValue: Space.s,
      description: '자식요소 간격',
      table: {
        type: {
          summary: 'number'
        },
        defaultValue: {summary: 16}
      }
    }
  }}
  component={VBox}
/>

<!-- Define template for Primary Story -->

export const Template = args => {
  return <VBox {...args} />;
};

# VBox

Flex를 이용한 레이아웃 컨테이너  
세로로 자식요소를 정렬해 주는 레이아웃 컨테이너 역할

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

### API

<ArgsTable story="Default" />
