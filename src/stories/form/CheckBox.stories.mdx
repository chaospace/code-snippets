import {ArgsTable, Canvas, Meta, Story, Props} from '@storybook/addon-docs';

import CheckBox from '@/ui/styled/form/CheckBox';
import {VBox} from '@/ui/styled/Box';

<Meta
  title="FormUI/CheckBox"
  argTypes={{
    children: {
      table: {disable: true},
      defaultValue: '지원여부'
    },
    color: {
      control: {type: 'color'},
      description: '라벨 컬러',
      table: {
        type: {summary: 'color'}
      }
    },
    fontWeight: {
      options: ['normal', 'bold'],
      control: {type: 'select'},
      description: '버튼 폰트 볼드 여부 ',
      table: {
        type: {summary: 'select'},
        defaultValue: 'normal'
      }
    },
    fontSize: {
      control: {type: 'text'},
      description: '폰트 크기',
      table: {
        type: {summary: 'string'}
      }
    },
    checkColor: {
      control: {type: 'color'},
      description: '체크마크 컬러',
      table: {
        type: {summary: 'color'}
      }
    },
    backgroundColor: {
      control: {type: 'color'},
      description: '체크마크 배경 컬러',
      table: {
        type: {summary: 'color'}
      }
    },
    border: {
      control: {type: 'text'},
      description: '체크마크 보더 스타일',
      table: {
        type: {summary: 'string'}
      }
    }
  }}
  component={CheckBox}
/>

export const Template = args => {
  const {children, border, fontSize, fontWeight, color, backgroundColor, ...rest} = args;
  return (
    <CheckBox style={{border, fontSize, fontWeight, color, backgroundColor}} {...rest}>
      {children}
    </CheckBox>
  );
};

# 인풋

인풋 컴포넌트

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

# Props

<ArgsTable story="Default" />

# 상태에 따른 스타일

<Canvas>
  <Story
    name="Select States"
    parameters={{
      controls: {disable: true},
      options: {showPanel: false}
    }}
  >
    <VBox gap="8">
      <label>disabled</label>
      <CheckBox disabled />
    </VBox>
  </Story>
</Canvas>
