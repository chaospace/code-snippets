import { css } from 'styled-components';

const fontStyle = css`
    
    html, body {
        font-size: 16px; //루트 폰트 스타일 적용
        font-family : "Noto Sans KR", "system-ui", sans-serif, -apple-system;
    }
`;

export default fontStyle;