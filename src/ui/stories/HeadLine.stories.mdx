import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';
import {HeadLine} from '@/ui/styled/Texts';
import {TypoProps} from '@/ui/styled/types/types';
import colors from '@/ui/colors';

<!-- 컴포넌트 정의   type: {required: true} -->

<Meta
  title="Example/HeadLine"
  argTypes={{
    children: {
      table: {disable: true},
      defaultValue: 'Hello World!'
    },
    $color: {
      control: {type: 'color'},
      defaultValue: colors.gray07,
      description: '폰트 컬러',
      table: {
        type: {summary: 'string'},
        defaultValue: {summary: colors.gray07}
      }
    },
    $type: {table: {disable: true}},
    bold: {
      table: {
        disable: true
      }
    }
  }}
  component={HeadLine}
/>

export const ComponentTemplate = args => {
  return <HeadLine {...args} />;
};

# HeadLine

h 태그를 쉽게 사용하기 위한 컴포넌트
스타일 정의한 타입값에 따라 폰트속성이 달라진다.

<Canvas>
  <Story name="Default">{ComponentTemplate.bind({})}</Story>
</Canvas>

## Props

<ArgsTable story="Default" />

## HeadLine Types

지원되는 타입 및 폰트 크기(왼쪽부터 h1 ~ h6)

<Canvas>
  <Story
    name="HeadLine Types"
    parameters={{
      controls: {disable: true},
      options: {showPanel: false}
    }}
  >
    <HeadLine $type="h1">Hello!</HeadLine>
    <HeadLine $type="h2">Hello!</HeadLine>
    <HeadLine $type="h3">Hello!</HeadLine>
    <HeadLine $type="h4">Hello!</HeadLine>
    <HeadLine $type="h5">Hello!</HeadLine>
    <HeadLine $type="h6">Hello!</HeadLine>
  </Story>
</Canvas>
